# bonus-games

## Docker

Start the test stack:

```bash
docker compose up --build
```

Services:

- Frontend: http://localhost:8080
- Backend: http://localhost:8081
- Postgres: localhost:5432

The temporary docker frontend lives in `room-template-tester-frontend/`.
